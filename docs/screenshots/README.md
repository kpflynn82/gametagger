# Browser verification captures

Generated by the checked-in Playwright workflow at 1440×1000, 768×900 and 390×900 (390×844 for result). Images are full-page captures, so file heights exceed the viewport. Reduced-motion mode avoids capturing a responsive transition in progress.

Overview captures show an empty workspace. Result captures show **Illustrative UI test data**, a synthetic image and an automated review mutation in an isolated temporary database. The displayed Puzzle approval verifies review persistence, not human truth or model recognition. Experiment capture displays real redacted historical counts and the explicit unmeasured comparison view; no live model experiment was run. No private game media or source descriptions are included.

Browser tests are workflow evidence, not independent code review or accessibility certification.
